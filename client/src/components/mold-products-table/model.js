export const form = [
  [
    {
      text: "Cavidades (PC ou KIT)",
      identity: "cavities",
      autoFocus: true,
      required: true,
      type: "integer"
    }
  ]
];

export default { form };
